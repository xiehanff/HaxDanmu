# Miscellaneous
*.class
*.log
*.pyc
*.swp
.DS_Store
.atom/
.build/
.buildlog/
.history
.svn/
.swiftpm/
migrate_working_dir/

# IntelliJ related
*.iml
*.ipr
*.iws
.idea/

# The .vscode folder contains launch configuration and tasks you configure in
# VS Code, so this line is commented out by default.
#.vscode/

# Flutter/Dart/Pub related
**/doc/api/
**/ios/Flutter/.last_build_id
.dart_tool/
.flutter-plugins-dependencies
.pub-cache/
.pub/
build/
coverage/

# This repository is a reusable package and its example participates in the
# minimum/current Flutter compatibility matrix. Do not pin either dependency
# graph to the Dart SDK that happened to generate a lockfile locally.
/pubspec.lock
/example/pubspec.lock

# Symbolication related
app.*.symbols

# Obfuscation related
app.*.map.json

# FVM Version Cache
.fvm/
